-- ============================================================================
-- 004_module8_carry.sql — الوحدة 8: الترحيل التلقائي للرصيد الافتتاحي (D9)
-- carried_from_session_id موجود منذ المخطط الأولي (001) — هنا فقط عمود
-- سبب التعديل الإجباري: إن عدّل الفريق الرصيد المرحَّل يدوياً وجب بيان السبب
-- (قرار المالك — قاعدة صفر تسامح في التوثيق).
-- قابلة لإعادة التطبيق (حارس وجود) — آمنة على قاعدة مطبقة جزئياً.
-- ============================================================================

ALTER TABLE reconciliation_sessions ADD COLUMN IF NOT EXISTS opening_adjust_reason text;
