-- ============================================================================
-- 003_module7_comments.sql — الوحدة 7: تعليقات الفروق
-- الجدول discrepancy_comments موجود منذ المخطط الأولي (001) — هنا فقط فهرس
-- الترقيم الزمني لأن الشاشة والتقرير يقرآن التعليقات بترتيب كتابتها.
-- قابلة لإعادة التطبيق (حارس وجود) — آمنة على قاعدة مطبقة جزئياً.
-- ============================================================================

CREATE INDEX IF NOT EXISTS ix_disc_comments_disc_time
  ON discrepancy_comments (discrepancy_id, id);
